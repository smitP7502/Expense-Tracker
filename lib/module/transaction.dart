// this the Transaction class for the grab the data for the app

class Transaction {
  final String id;
  final String title;
  final int amount;
  final DateTime date;

  Transaction(this.id, this.title, this.amount, this.date);
}
